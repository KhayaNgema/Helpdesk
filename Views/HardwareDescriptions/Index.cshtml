@model IEnumerable<Helpdesk.Models.HardwareDescription>

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "Hardware Descriptions Index";
}

<h2>Hardware Descriptions Index</h2>

<p>
    @Html.ActionLink("Create New", "Create", null, new { @class = "btn btn-primary" })
</p>

<div class="table-responsive">
    <table class="table table-striped table-bordered">
        <thead class="thead-dark">
            <tr>
                <th>Hardware Description Name</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@Html.DisplayFor(modelItem => item.HardwareDescriptionName)</td>
                    <td>
                        @Html.ActionLink("Edit", "Edit", new { id = item.HardwareDescriptionId }, new { @class = "btn btn-primary btn-sm" }) |
                        @Html.ActionLink("Details", "Details", new { id = item.HardwareDescriptionId }, new { @class = "btn btn-info btn-sm" }) |
                        @Html.ActionLink("Delete", "Delete", new { id = item.HardwareDescriptionId }, new { @class = "btn btn-danger btn-sm" })
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
