@model IEnumerable<Helpdesk.Models.VirtualizedPlatform>

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "Virtualized Platform Index";
}

<h2>Virtualized Platform Index</h2>

<p>
    @Html.ActionLink("Create New", "Create", null, new { @class = "btn btn-primary" })
</p>

<div class="table-responsive">
    <table class="table table-striped table-bordered">
        <thead class="thead-dark">
            <tr>
                <th>Virtualized Platform Name</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@Html.DisplayFor(modelItem => item.VirtualizedPlatformName)</td>
                    <td>
                        @Html.ActionLink("Edit", "Edit", new { id = item.VirtualizedPlatformId }, new { @class = "btn btn-primary btn-sm" }) |
                        @Html.ActionLink("Details", "Details", new { id = item.VirtualizedPlatformId }, new { @class = "btn btn-info btn-sm" }) |
                        @Html.ActionLink("Delete", "Delete", new { id = item.VirtualizedPlatformId }, new { @class = "btn btn-danger btn-sm" })
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
